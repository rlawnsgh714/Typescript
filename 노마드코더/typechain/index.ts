const name = "junho",
  age = 24,
  mail = "junho07140714";

const sayhi = (name, age, mail) => {
  console.log(`Hello ${name}, you are ${age}, you are a ${mail}`);
};

sayhi(name, age, mail);

export {};
